/**
 * User Management Workflows Index
 * Barrel exports cho tất cả workflow components
 */

// Export RolePromotionWorkflow component
export { RolePromotionWorkflow } from './role-promotion-workflow';

// Export types nếu cần
export type {
  // Có thể export interfaces nếu cần sử dụng bên ngoài
} from './role-promotion-workflow';
